---
phase: 03-display-feedback-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/components.css
  - src/components/Progress/Progress.tsx
  - src/components/Progress/Progress.stories.tsx
  - src/components/Progress/Progress.test.tsx
  - src/components/Progress/index.ts
  - src/components/Skeleton/Skeleton.tsx
  - src/components/Skeleton/Skeleton.stories.tsx
  - src/components/Skeleton/Skeleton.test.tsx
  - src/components/Skeleton/index.ts
  - src/components/Toast/Toast.tsx
  - src/components/Toast/Toast.stories.tsx
  - src/components/Toast/Toast.test.tsx
  - src/components/Toast/index.ts
autonomous: true
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - SKEL-01
  - SKEL-02
  - TOAST-01
  - TOAST-02
  - TOAST-03

must_haves:
  truths:
    - "Progress bar shows a track with fill that animates width based on value 0-100"
    - "Skeleton shows a shimmer animation sweeping across the element"
    - "Toast slides in from bottom with emphasized easing, shows lime bg, and auto-dismisses"
  artifacts:
    - path: "src/styles/components.css"
      provides: "shimmer and toast-enter keyframe animations"
      contains: "@keyframes shimmer"
    - path: "src/components/Progress/Progress.tsx"
      provides: "Progress bar with track and fill"
      exports: ["Progress"]
    - path: "src/components/Skeleton/Skeleton.tsx"
      provides: "Skeleton loading placeholder with shimmer"
      exports: ["Skeleton"]
    - path: "src/components/Toast/Toast.tsx"
      provides: "Toast notification with auto-dismiss"
      exports: ["Toast"]
  key_links:
    - from: "src/components/Skeleton/Skeleton.tsx"
      to: "src/styles/components.css"
      via: "skeleton-shimmer CSS class"
      pattern: "skeleton-shimmer"
    - from: "src/components/Toast/Toast.tsx"
      to: "src/styles/components.css"
      via: "toast-enter CSS class"
      pattern: "toast-enter"
    - from: "src/components/Toast/Toast.tsx"
      to: "React useEffect"
      via: "setTimeout for auto-dismiss"
      pattern: "setTimeout.*onDismiss"
---

<objective>
Implement Progress, Skeleton, and Toast -- three feedback components that require CSS keyframe animations (Skeleton shimmer, Toast slide-in) and stateful behavior (Toast auto-dismiss timer).

Purpose: These components provide loading indicators (Progress, Skeleton) and transient notifications (Toast) for the design system. Toast is the most behaviorally complex component in this phase.
Output: Three component folders plus two new @keyframes blocks in components.css.
</objective>

<execution_context>
@C:/Users/NILAMZ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NILAMZ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-display-feedback-components/03-RESEARCH.md
@src/styles/components.css (add keyframes here)
@src/styles/globals.css (token references)
@src/components/Button/Button.tsx (pattern reference)
@src/components/Checkbox/Checkbox.stories.tsx (useState story pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS keyframes and create Progress and Skeleton components</name>
  <files>
    src/styles/components.css
    src/components/Progress/Progress.tsx
    src/components/Progress/Progress.stories.tsx
    src/components/Progress/Progress.test.tsx
    src/components/Progress/index.ts
    src/components/Skeleton/Skeleton.tsx
    src/components/Skeleton/Skeleton.stories.tsx
    src/components/Skeleton/Skeleton.test.tsx
    src/components/Skeleton/index.ts
  </files>
  <action>
    **components.css -- append two keyframe blocks:**

    ```css
    /* -- Skeleton shimmer ---------------------------------------- */
    @keyframes shimmer {
      0%   { background-position: -200% 0; }
      100% { background-position:  200% 0; }
    }
    .skeleton-shimmer {
      background: linear-gradient(
        90deg,
        rgba(143, 238, 170, 0.04) 25%,
        rgba(143, 238, 170, 0.10) 50%,
        rgba(143, 238, 170, 0.04) 75%
      );
      background-size: 200% 100%;
      animation: shimmer 1.5s infinite ease-in-out;
      border-radius: var(--truf-radius-sm);
    }

    /* -- Toast slide-in ------------------------------------------ */
    @keyframes toast-enter {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    .toast-enter {
      animation: toast-enter var(--truf-duration-slow) var(--truf-ease-emphasized) forwards;
    }
    ```

    DO NOT modify existing CSS blocks (input-focus, switch, checkbox). Append only.

    **Progress component (Progress.tsx):**
    - Props: `value` (number, 0-100), `className` (optional), `style` (optional)
    - Clamp value: `Math.min(100, Math.max(0, value))`
    - Outer track div: `className="w-full h-[4px] rounded-full overflow-hidden"`, inline style `background: 'var(--surface-2)'`
    - Inner fill div: `className="h-full bg-lime-500 rounded-full"`, inline style `width: \`${clamped}%\``, `transition: \`width var(--truf-duration-base) var(--truf-ease-standard)\``
    - No state needed -- purely controlled by parent

    **Progress stories:**
    - Meta: title 'Components/Progress', tags ['autodocs'], layout 'padded'
    - Stories: Empty (value=0), Half (value=50), Full (value=100)
    - Story: Animated -- use render + useState + useEffect to animate from 0 to 75 over time

    **Progress test:**
    - Test: renders track element
    - Test: fill width matches value prop (check inline style)
    - Test: clamps value above 100 to 100%
    - Test: clamps value below 0 to 0%

    **Skeleton component (Skeleton.tsx):**
    - Props: `className` (optional, for width/height), `style` (optional), rest spread on div
    - Render `<div>` with className: `skeleton-shimmer` + passed className
    - Default inline style: `height: '20px'` (overridable by className or style prop)
    - No internal state

    **Skeleton stories:**
    - Meta: title 'Components/Skeleton', tags ['autodocs'], layout 'padded'
    - Stories: Default (single line), TextBlock (3 skeletons stacked with varying widths), CardSkeleton (skeleton inside a Card-like container)

    **Skeleton test:**
    - Test: renders with skeleton-shimmer class
    - Test: accepts custom className for sizing
    - Test: renders as div element

    **index.ts** for both: standard re-export
  </action>
  <verify>Run `npx vitest run src/components/Progress src/components/Skeleton --reporter=verbose` -- all tests pass</verify>
  <done>Progress renders track+fill with animated width transition. Skeleton shows shimmer via CSS keyframe animation. Both have passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create Toast component with auto-dismiss and stories/tests</name>
  <files>
    src/components/Toast/Toast.tsx
    src/components/Toast/Toast.stories.tsx
    src/components/Toast/Toast.test.tsx
    src/components/Toast/index.ts
  </files>
  <action>
    **Toast component (Toast.tsx):**
    - Props interface: `message` (string), `visible` (boolean), `duration` (number, default 3000), `onDismiss` (optional callback), `className` (optional)
    - Controlled component pattern (parent manages visible state, matches Switch/Checkbox pattern)
    - useEffect: when `visible` is true, set setTimeout for `duration` ms that calls `onDismiss?.()`. Return cleanup `clearTimeout`. Dependencies: `[visible, duration, onDismiss]`
    - If `!visible`, return null (don't render anything)
    - Outer div: `className="fixed bottom-6 left-1/2 -translate-x-1/2 z-[600]"` (z-[600] matches --truf-z-toast, NOT mapped in @theme so use arbitrary value per Pitfall 2)
    - Inner div: `className="toast-enter bg-lime-500 text-forest-900 font-semibold rounded-pill px-5 py-3 text-sm shadow-md"`
    - Message rendered as text content inside inner div

    **Toast stories (Toast.stories.tsx):**
    - Meta: title 'Components/Toast', tags ['autodocs'], layout 'fullscreen'
    - Story: Default -- use render + useState. Render a Button that sets visible=true. Toast with `onDismiss={() => setVisible(false)}`. Shows interaction pattern.
    - Story: LongDuration -- same pattern but duration=5000
    - Import Button from existing component for the trigger button

    **Toast test (Toast.test.tsx):**
    - Test: does not render when visible=false (container has no children)
    - Test: renders message text when visible=true
    - Test: has toast-enter class when visible
    - Test: calls onDismiss after duration (use vi.useFakeTimers + vi.advanceTimersByTime)
    - Test: cleans up timer on unmount (use vi.useFakeTimers, render then unmount, advance time, verify onDismiss NOT called)
    - IMPORTANT: Use `vi.useFakeTimers()` and `vi.useRealTimers()` in beforeEach/afterEach for timer tests

    **index.ts:** `export { Toast } from './Toast'`
  </action>
  <verify>Run `npx vitest run src/components/Toast --reporter=verbose` -- all tests pass including timer-based tests</verify>
  <done>Toast renders with lime bg and pill radius, slides in via toast-enter animation, and auto-dismisses after configurable duration. Timer cleanup works correctly. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/components/Progress src/components/Skeleton src/components/Toast --reporter=verbose` -- all tests pass
- `npm run storybook` -- Progress, Skeleton, Toast all appear under Components/
- Skeleton shimmer animation is visible (not static gradient)
- Toast slides in from bottom (not instant appear)
- Toast disappears after ~3 seconds
</verification>

<success_criteria>
- Progress bar fills proportionally to value prop with smooth transition
- Skeleton displays sweeping shimmer animation
- Toast slides in from bottom with emphasized easing, renders lime bg with pill radius, auto-dismisses after duration
- All timer tests pass with fake timers
</success_criteria>

<output>
After completion, create `.planning/phases/03-display-feedback-components/03-02-SUMMARY.md`
</output>
