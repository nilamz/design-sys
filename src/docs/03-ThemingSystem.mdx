import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Theming System" />

<div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: '0.75rem', color: 'var(--truf-lime-500)', letterSpacing: '0.14em', textTransform: 'uppercase', marginBottom: '0.75rem' }}>
  Theming System
</div>
<h1 style={{ fontFamily: "'Fraunces', 'Times New Roman', serif", fontSize: 60, lineHeight: 1.02, fontWeight: 400, letterSpacing: '-0.04em', margin: '0 0 1.5rem', color: 'var(--text-primary)' }}>{'Two '}<em style={{ fontStyle: 'italic', color: 'var(--truf-lime-400)', fontWeight: 300 }}>{'modes.'}</em>{' Infinite overrides.'}</h1>

<p style={{ fontFamily: "'Inter', sans-serif", fontSize: '1.1rem', lineHeight: 1.7, color: 'var(--text-secondary)', maxWidth: '42rem', marginBottom: '3rem' }}>
Truf ships with a dark default (the brand signature) and a light mode for daytime and printable contexts. Both themes are expressed as CSS variables and can be extended without forking the component library.
</p>

## Theme Structure

The theme object follows MUI conventions — `theme.palette`, `theme.typography`, `theme.spacing()`. Developers consume theme tokens through CSS custom properties; no raw hex values should ever appear in product code.

```typescript
// theme/index.ts
export const theme = {
  palette: {
    mode: 'dark',
    primary: {
      main:         '#0B2E24',  // forest.800
      contrastText: '#F5F1E8',
    },
    secondary: {
      main:         '#2DD653',  // lime.500
      contrastText: '#061A13',
    },
    background: {
      default: '#061A13',
      paper:   '#0B2E24',
    },
    text: {
      primary:   '#F5F1E8',
      secondary: '#9FE5C9',
    },
  },
  typography: {
    fontFamily: 'Inter, system-ui, sans-serif',
    h1: { fontFamily: 'Fraunces, serif', fontSize: '3.75rem', lineHeight: 1.02 },
  },
  spacing: (factor: number) => `${factor * 4}px`,
  shape: { borderRadius: 12 },
}
```

## Overriding the Theme

Developers can override any token locally. Overrides cascade deeply without breaking ancestor styles.

```tsx
// Override accent color for a marketing page
<TrufThemeProvider theme={{
  palette: { secondary: { main: '#34E06A' } }
}}>
  <LandingPage />
</TrufThemeProvider>
```

## Light vs Dark — Token Mapping

<div style={{ overflowX: 'auto', marginTop: '1.5rem', marginBottom: '2rem', borderRadius: 'var(--radius-md)', border: '1px solid var(--border)', overflow: 'hidden' }}>
  <table style={{ width: '100%', borderCollapse: 'collapse', fontFamily: "'Inter', sans-serif", fontSize: '0.875rem' }}>
    <thead>
      <tr>
        <th style={{ textAlign: 'left', padding: '0.75rem 1rem', background: 'var(--surface-2)', color: 'var(--text-muted)', fontFamily: "'JetBrains Mono', monospace", fontSize: '0.7rem', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.1em', borderBottom: '1px solid var(--border)' }}>Semantic Token</th>
        <th style={{ textAlign: 'left', padding: '0.75rem 1rem', background: 'var(--surface-2)', color: 'var(--text-muted)', fontFamily: "'JetBrains Mono', monospace", fontSize: '0.7rem', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.1em', borderBottom: '1px solid var(--border)' }}>Dark Value</th>
        <th style={{ textAlign: 'left', padding: '0.75rem 1rem', background: 'var(--surface-2)', color: 'var(--text-muted)', fontFamily: "'JetBrains Mono', monospace", fontSize: '0.7rem', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.1em', borderBottom: '1px solid var(--border)' }}>Light Value</th>
      </tr>
    </thead>
    <tbody>
      {[
        { token: 'bg.default', dark: '#061A13', light: '#F5F1E8' },
        { token: 'bg.surface', dark: '#0B2E24', light: '#FFFFFF' },
        { token: 'text.primary', dark: '#F5F1E8', light: '#061A13' },
        { token: 'text.secondary', dark: '#9FE5C9', light: '#135C46' },
        { token: 'accent', dark: '#2DD653', light: '#0B2E24' },
        { token: 'border', dark: 'rgba(143,238,170,.12)', light: 'rgba(11,46,36,.10)' },
      ].map((row, i) => (
        <tr key={row.token} style={{ borderBottom: i < 5 ? '1px solid var(--border)' : 'none' }}>
          <td style={{ padding: '0.75rem 1rem', background: 'var(--surface)', color: 'var(--truf-lime-400)', fontFamily: "'JetBrains Mono', monospace", fontSize: '0.8rem' }}>{row.token}</td>
          <td style={{ padding: '0.75rem 1rem', background: 'var(--surface)' }}>
            <span style={{ display: 'inline-flex', alignItems: 'center', gap: '0.5rem' }}>
              <span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 4, background: row.dark, border: '1px solid var(--border)', flexShrink: 0 }} />
              <code style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: '0.8rem', color: 'var(--text-secondary)', background: 'transparent' }}>{row.dark}</code>
            </span>
          </td>
          <td style={{ padding: '0.75rem 1rem', background: 'var(--surface)' }}>
            <span style={{ display: 'inline-flex', alignItems: 'center', gap: '0.5rem' }}>
              <span style={{ display: 'inline-block', width: 14, height: 14, borderRadius: 4, background: row.light, border: '1px solid var(--border)', flexShrink: 0 }} />
              <code style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: '0.8rem', color: 'var(--text-secondary)', background: 'transparent' }}>{row.light}</code>
            </span>
          </td>
        </tr>
      ))}
    </tbody>
  </table>
</div>

### How it works

The dark theme is the default — no `data-theme` attribute needed. Setting `data-theme="light"` on any element swaps all semantic CSS variables within that subtree:

```css
/* Dark (default) */
:root {
  --bg: var(--truf-forest-900);
  --text-primary: var(--truf-cream);
  --accent: var(--truf-lime-500);
}

/* Light override */
[data-theme="light"] {
  --bg: var(--truf-cream);
  --text-primary: var(--truf-forest-900);
  --accent: var(--truf-forest-800);
}
```

Use Storybook's theme toggle (top toolbar) to preview components in both modes.
