---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - .storybook/main.ts
  - .storybook/preview.ts
  - .gitignore
  - src/components/.gitkeep
  - src/index.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Running npm run dev starts a Vite dev server with no errors"
    - "Running npm run storybook opens Storybook 8 with no errors"
    - "The project compiles TypeScript without errors"
    - "Tailwind CSS v4 processes utility classes via the Vite plugin"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with react, vite, tailwindcss, storybook dependencies"
      contains: "tailwindcss"
    - path: "vite.config.ts"
      provides: "Vite config with React and Tailwind v4 plugins"
      contains: "tailwindcss"
    - path: ".storybook/main.ts"
      provides: "Storybook config with react-vite framework"
      contains: "@storybook/react-vite"
    - path: ".storybook/preview.ts"
      provides: "Storybook preview with theme toggle decorator"
      contains: "withThemeByDataAttribute"
    - path: "src/index.ts"
      provides: "Barrel export entry point (placeholder)"
    - path: "src/components/.gitkeep"
      provides: "Empty components directory placeholder"
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugin registration"
      pattern: "tailwindcss\\(\\)"
    - from: ".storybook/main.ts"
      to: "@storybook/react-vite"
      via: "framework config"
      pattern: "@storybook/react-vite"
    - from: ".storybook/preview.ts"
      to: "src/styles/globals.css"
      via: "CSS import"
      pattern: "import.*globals\\.css"
---

<objective>
Scaffold the Vite + React 18 + TypeScript project with Tailwind CSS v4 and Storybook 8 installed and configured.

Purpose: Establishes the runnable project foundation that all token and component work depends on.
Output: A working project where `npm run dev` and `npm run storybook` both launch without errors, Tailwind v4 is wired via the Vite plugin, and Storybook has the dark/light theme toggle decorator ready.
</objective>

<execution_context>
@C:/Users/NILAMZ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NILAMZ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React 18 + TypeScript project and install all dependencies</name>
  <files>
    package.json
    tsconfig.json
    tsconfig.app.json
    tsconfig.node.json
    vite.config.ts
    index.html
    src/main.tsx
    src/App.tsx
    src/vite-env.d.ts
    .gitignore
  </files>
  <action>
    1. Run `npm create vite@latest . -- --template react-ts` from the project root (F:\My-Claude-2026\Design-Sys) to scaffold into the existing directory. If the tool requires a different invocation for an existing directory, use the appropriate flag or scaffold into a temp name and move files.

    2. Run `npm install` to install base dependencies.

    3. Install Tailwind CSS v4 with Vite plugin:
       `npm install tailwindcss @tailwindcss/vite`

    4. Update `vite.config.ts` to register both plugins:
       ```typescript
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';
       import tailwindcss from '@tailwindcss/vite';

       export default defineConfig({
         plugins: [react(), tailwindcss()],
       });
       ```

    5. Update `index.html` to add Google Fonts CDN links in the `<head>`:
       - Inter (weights 300-800)
       - JetBrains Mono (weights 400,700)
       - Fraunces (weights 300-800)

    6. Create placeholder files for the project structure:
       - `src/components/.gitkeep` (empty directory placeholder)
       - `src/index.ts` with a comment: `// Barrel export — components added in Phase 2+`
       - Create `src/styles/` directory (globals.css will be created in Plan 02)
       - Create `src/tokens/` directory (token files will be created in Plan 02)

    7. Clean up Vite template files: Remove default `src/App.css`, `src/index.css`, and any default Vite SVG assets. Update `src/App.tsx` to a minimal component that just renders a div with `className="bg-[var(--bg)] text-[var(--text-primary)] min-h-screen p-8"` and an h1 saying "Truf Design System". Update `src/main.tsx` to import `./styles/globals.css` instead of the default CSS import (the actual globals.css file will be created in Plan 02, but the import path should be ready).

    NOTE: Do NOT create a `tailwind.config.js` or `tailwind.config.ts` file. Tailwind v4 uses CSS-first config via `@theme` in the CSS file, not a JS config. Do NOT use `@tailwind` directives — v4 uses `@import "tailwindcss"`.
  </action>
  <verify>
    - `ls package.json vite.config.ts tsconfig.json src/main.tsx src/App.tsx` all exist
    - `cat vite.config.ts` shows both `react()` and `tailwindcss()` plugins
    - `cat package.json` shows `tailwindcss` and `@tailwindcss/vite` in dependencies
    - `ls src/components/.gitkeep src/index.ts` exist
    - No `tailwind.config.js` or `tailwind.config.ts` file exists
  </verify>
  <done>
    Project is scaffolded with Vite + React 18 + TypeScript. Tailwind CSS v4 is installed and registered as a Vite plugin. Directory structure matches user specification. Google Fonts CDN links are in index.html. No Tailwind v3 patterns present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install and configure Storybook 8 with theme toggle</name>
  <files>
    .storybook/main.ts
    .storybook/preview.ts
    package.json
  </files>
  <action>
    1. Run `npx storybook@latest init` to auto-detect Vite and install Storybook 8 with `@storybook/react-vite` framework. Accept defaults. If prompted about ESLint plugin, decline.

    2. Verify `.storybook/main.ts` uses `framework: { name: '@storybook/react-vite' }`. If it auto-selected webpack, fix it.

    3. Install the themes addon if not already present:
       `npm install --save-dev @storybook/addon-themes`

    4. Update `.storybook/main.ts` to:
       ```typescript
       import type { StorybookConfig } from '@storybook/react-vite';

       const config: StorybookConfig = {
         stories: ['../src/**/*.stories.@(ts|tsx)'],
         addons: [
           '@storybook/addon-essentials',
           '@storybook/addon-themes',
         ],
         framework: {
           name: '@storybook/react-vite',
           options: {},
         },
       };

       export default config;
       ```

    5. Update `.storybook/preview.ts` to:
       ```typescript
       import type { Preview } from '@storybook/react-vite';
       import { withThemeByDataAttribute } from '@storybook/addon-themes';
       import '../src/styles/globals.css';

       const preview: Preview = {
         decorators: [
           withThemeByDataAttribute({
             themes: {
               dark: '',
               light: 'light',
             },
             defaultTheme: 'dark',
             attributeName: 'data-theme',
           }),
         ],
         parameters: {
           backgrounds: { disable: true },
           layout: 'centered',
         },
       };

       export default preview;
       ```

    6. Remove any auto-generated example stories from `src/stories/` (Storybook init creates sample Button stories — delete the entire `src/stories/` folder if it exists, since we will build our own components).

    7. Create a minimal placeholder `src/styles/globals.css` so that the Storybook preview import doesn't fail at startup:
       ```css
       /* Placeholder — full token system added in Plan 02 */
       @import "tailwindcss";
       ```

    NOTE: Storybook's `withThemeByDataAttribute` sets `data-theme` on the preview iframe's html element. Dark theme is default (empty string means no attribute = `:root` styles apply). Light theme sets `data-theme="light"`. Set `backgrounds: { disable: true }` to prevent Storybook's built-in backgrounds from conflicting with our CSS-based theming.
  </action>
  <verify>
    - `npx storybook --version` shows 8.x (or latest stable)
    - `cat .storybook/main.ts` shows `@storybook/react-vite` framework
    - `cat .storybook/main.ts` shows `@storybook/addon-themes` in addons
    - `cat .storybook/preview.ts` shows `withThemeByDataAttribute` import and config
    - `cat .storybook/preview.ts` shows `import '../src/styles/globals.css'`
    - `ls src/stories/ 2>/dev/null` returns nothing (example stories removed)
    - `npm run build` completes without errors (verifies TypeScript + Vite + Tailwind pipeline works)
  </verify>
  <done>
    Storybook 8 is installed with @storybook/react-vite framework. Theme toggle decorator is configured for dark/light switching via data-theme attribute. Example stories are removed. Both `npm run dev` and `npm run storybook` commands are available and runnable (full verification deferred to after Plan 02 creates the complete globals.css).
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts Vite dev server (may show minimal content until Plan 02 adds tokens)
2. `npm run build` compiles without TypeScript errors
3. `cat vite.config.ts` confirms both react() and tailwindcss() plugins
4. `cat .storybook/main.ts` confirms @storybook/react-vite framework and addon-themes
5. `cat .storybook/preview.ts` confirms withThemeByDataAttribute decorator with dark default
6. Directory structure matches: src/tokens/, src/styles/, src/components/, .storybook/
7. No tailwind.config.js or tailwind.config.ts exists
</verification>

<success_criteria>
- Vite dev server starts without errors on `npm run dev`
- `npm run build` succeeds
- Storybook is runnable via `npm run storybook`
- Tailwind v4 is registered as Vite plugin (no PostCSS config, no JS config)
- Storybook has dark/light theme toggle via addon-themes
- Project structure matches user-specified layout
- No Tailwind v3 patterns present (no @tailwind directives, no tailwind.config.js)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
